@model IEnumerable<WIPCream.Presentation.WUI.Models.UserRole.UserRoleUIDTO>

@{
    ViewBag.Title = "Index";
}
@Html.Partial("_Navigation")

<div id="wrapper">
    <div class="row content">
        <div class="col-xs-12">
            <div class="hpanel hgreen">
                <div class="panel-body">
                    <div class="hpanel">
                        <h2>User Roles</h2>

                        <p>
                            @Html.ActionLink("Create New", "Create")
                        </p>
                        <table class="table">
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.Users.UserName)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Users.UserRole)
                                </th>
                                <th></th>
                            </tr>

                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Users.UserName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.RoleId)
                                    </td>
                                    <td>
                                        @Html.ActionLink("Edit", "Edit", new { id = item.UserRoleId }) |
                                        @Html.ActionLink("Details", "Details", new { id = item.UserRoleId }) |
                                        @Html.ActionLink("Delete", "Delete", new { id = item.UserRoleId })
                                    </td>
                                </tr>
                            }

                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>